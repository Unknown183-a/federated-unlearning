# Daily work report — 2026-09-02

**Phase:** phase-04-full-retraining-baseline
**Time spent:** ~3 hours

## What I did

- Migrated the base FL experiment from MNIST/CNN (pipeline-validation) to
  the real CIFAR-100 + ResNet-18 setup used going forward.
- Implemented `src/baselines/full_retraining.py::run_full_retraining`,
  filling the Phase 04 scaffold by reusing `FederatedServer` with the
  forget client excluded.
- Imported pre-existing `original_model.pt`/`retrained_model.pt`
  checkpoints (trained locally before this refactor) and evaluated them
  through the new pipeline (`experiments/evaluate_legacy_checkpoints.py`),
  reconstructing the legacy forget-client (client 0) partition exactly
  to get a real forget-client accuracy for `M_retrain`.

## What changed in the repo

- New: `src/data/cifar.py`, `src/models/resnet.py`,
  `configs/cifar100_fl.yaml`, `experiments/run_cifar100_fl.py`,
  `experiments/run_full_retraining.py`,
  `experiments/evaluate_legacy_checkpoints.py`
- Modified: `src/models/cnn.py` (build_model now dispatches to resnet18),
  `src/baselines/full_retraining.py` (implemented),
  `configs/unlearning.yaml` (base_checkpoint path fixed),
  `STATUS.md`, `README.md`, `phases/phase-04-full-retraining-baseline/PHASE.md`

## Blockers / open questions

- M_retrain (31.86% test acc.) is notably lower than M_old (60.02%)
  despite more training rounds/local epochs — likely FedAvg client
  drift from `local_epochs=10` on non-IID data, but the original
  per-round logs weren't preserved to rule out a training-instability
  explanation instead. Worth revisiting in the final report if time
  allows; not a blocker for Phase 05.
- CPU-only training is slow for this model/dataset size — worth
  moving to GPU (Colab or similar) for Phase 05-07 if available.

## Next session

- Start Phase 05 (Gradient Ascent) in
  `phases/phase-05-gradient-ascent/PHASE.md`.

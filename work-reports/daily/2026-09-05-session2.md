# Daily work report — 2026-09-05 (session 2)

**Phase:** phase-05-gradient-ascent
**Time spent:** ~1.5 hours

## What I did

- Implemented `GradientAscentUnlearner.unlearn()`, adding gradient
  clipping after discovering the unbounded ascent objective diverges
  to NaN without it.
- Added `tests/test_gradient_ascent.py` (2 tests, both passing; all 8
  repo tests pass).
- Built `experiments/run_gradient_ascent.py` to produce a real
  M_unlearn and compare it against M_old.
- Ran it on GPU (Colab) against real CIFAR-100 and M_old, downloaded
  results, wrote up final numbers in STATUS.md/PHASE.md.

## What changed in the repo

- `src/unlearning/gradient_ascent.py` — implemented
- `configs/unlearning.yaml` — added `max_grad_norm` to `gradient_ascent`
- New: `experiments/run_gradient_ascent.py`, `tests/test_gradient_ascent.py`
- `artifacts/experiments/unlearning_ga_kd/gradient_ascent/` — real
  M_unlearn checkpoint + metrics (not committed to git, per .gitignore)
- `STATUS.md`, `README.md`,
  `phases/phase-05-gradient-ascent/PHASE.md` — updated with results

## Blockers / open questions

- None. Result confirms the expected GA weakness: cheap but imprecise
  forgetting with real collateral damage to overall accuracy, vs.
  M_retrain's expensive-but-clean result. Good material for the final
  report's cost/precision discussion.

## Next session

- Start Phase 06 (Knowledge Distillation) in
  `phases/phase-06-knowledge-distillation/PHASE.md`.
